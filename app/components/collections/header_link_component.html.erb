<% if @collection.druid %>
  <%= link_to collection_path(@collection.druid) do %>
    <%= render SdrViewComponents::Elements::HeadingComponent.new(level:, text: @collection.title) %>
  <% end %>
<% else %>
  <%= render SdrViewComponents::Elements::HeadingComponent.new(level:, text: @collection.title) %>
<% end %>
