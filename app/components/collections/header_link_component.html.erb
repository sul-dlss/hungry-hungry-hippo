<% if @collection.druid %>
  <%= link_to collection_path(@collection.druid) do %>
    <%= render Elements::HeadingComponent.new(level:, text: @collection.title, classes:) %>
  <% end %>
<% else %>
  <%= render Elements::HeadingComponent.new(level:, text: @collection.title, classes:) %>
<% end %>
