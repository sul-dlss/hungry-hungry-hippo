<%= render Elements::ButtonLinkComponent.new(link: content_file_path(content_file), variant: :'outline-primary', bordered: false, data: { turbo_method: :delete, turbo_frame: dom_id(content_file.content, 'show') }, classes: 'py-0') do %>
  <%= helpers.delete_icon %><span class="visually-hidden">Remove</span>
<% end %>
