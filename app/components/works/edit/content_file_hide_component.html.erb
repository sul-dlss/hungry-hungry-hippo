<%= tag.turbo_frame id: dom_id(content_file, 'hide') do %>
  <%= form_with model: content_file, id: form_id do |form| %>
    <%= render Elements::Forms::CheckboxComponent.new(form:, field_name: :hide, hidden_label: true, label: 'Hide this file', data: { controller: 'form-submit', action: 'form-submit#submit' }) %>
  <% end %>
<% end %>
