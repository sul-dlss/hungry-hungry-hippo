<%= tag.turbo_frame id: dom_id(content_file, 'hide') do %>
  <%= form_with model: content_file, id: form_id do |form| %>
    <%= render SdrViewComponents::Forms::CheckboxComponent.new(form:, field_name: :hide, label_hidden: true, label_text: 'Hide this file', data: { controller: 'form-submit', action: 'form-submit#submit' }) %>
  <% end %>
<% end %>
