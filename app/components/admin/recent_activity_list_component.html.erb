<%= render Elements::Tables::TableComponent.new(id: 'recent-activity-table', classes: 'mb-5', label:, show_label: false, empty_message:) do |component| %>
  <% component.with_headers(headers) %>
  <% rows.each do |row| %>
    <% component.with_row(values: row) %>
  <% end %>
<% end %>
