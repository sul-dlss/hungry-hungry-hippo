<%= render Elements::Tables::TableComponent.new(
      id:, label:, empty_message:
    ) do |component| %>
  <% component.with_headers([
                              { label: 'Title' },
                              { label: 'Collection' },
                              { label: 'Deposit status' },
                              { label: 'Druid' },
                              { label: 'Last modified' }
                            ]) %>
  <% works.each do |work| %>
    <% component.with_row(values: values_for(work), id: id_for(work)) %>
  <% end %>
<% end %>
