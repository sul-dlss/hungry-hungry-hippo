<%= turbo_frame_tag 'history' do %>
  <%= render Elements::Tables::TableComponent.new(id: 'history-table', classes: 'mb-5') do |component| %>
    <% component.with_caption do %><%= render Show::TableHeadingComponent.new(text: 'History') %><% end %>
    <% @events.each do |event| %>
      <% component.with_row(label: event.label, values: [event.who, event.timestamp, event.description]) %>
    <% end %>
  <% end %>
<% end %>
