<%= render Emails::SalutationComponent.new(user: @user) %>

<p>The item <%= @work.title %> has been deposited in the <%= @collection.title %> collection by <%= @work.user.name %>, a collection manager, or an SDR administrator.

<%= render Emails::ContactComponent.new %>

<%= render Emails::SignatureComponent.new %>
