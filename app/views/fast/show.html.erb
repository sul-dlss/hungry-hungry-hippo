<% @suggestions.map do |suggestion| %>
  <%= tag.li class: 'list-group-item', role: 'option', data: { autocomplete_value: suggestion.values.first, autocomplete_label: suggestion.keys.first } do %><%= suggestion.keys.first %><% end %>
<% end %>
